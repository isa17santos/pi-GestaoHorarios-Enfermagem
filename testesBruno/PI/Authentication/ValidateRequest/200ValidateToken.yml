info:
  name: 200ValidateToken
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/password-recovery/validate-token"
  body:
    type: json
    data: "{\r

      \    \"token\" : \"{{password_reset_token}}\",\r

      \    \"email\": \"miguel.ferreira@example.pt\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
