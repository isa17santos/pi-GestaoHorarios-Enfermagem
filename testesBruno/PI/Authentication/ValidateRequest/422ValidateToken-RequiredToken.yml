info:
  name: 422ValidateToken-RequiredToken
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/password-recovery/validate-token"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: "{\r

      \    \"email\": \"miguel.ferreira@example.pt\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
