info:
  name: 200Logout
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/logout"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{api_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
