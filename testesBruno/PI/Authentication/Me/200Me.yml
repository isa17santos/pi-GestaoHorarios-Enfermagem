info:
  name: 200Me
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/me"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{api_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
