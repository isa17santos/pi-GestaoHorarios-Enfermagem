info:
  name: LoginHeadNurse
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/login"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: "{\r

      \    \"email\": \"ana.antunes@example.pt\",\r

      \    \"password\": \"password\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
