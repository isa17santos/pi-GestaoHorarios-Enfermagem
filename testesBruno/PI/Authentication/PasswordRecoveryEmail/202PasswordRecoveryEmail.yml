info:
  name: 202PasswordRecoveryEmail
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/password-recovery/email"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: "{\r

      \    \"email\": \"miguel.ferreira@example.pt\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
