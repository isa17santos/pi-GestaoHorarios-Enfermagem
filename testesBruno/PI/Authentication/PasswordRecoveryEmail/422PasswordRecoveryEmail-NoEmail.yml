info:
  name: 422PasswordRecoveryEmail-NoEmail
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/password-recovery/email"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: "{\r\n    \r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
