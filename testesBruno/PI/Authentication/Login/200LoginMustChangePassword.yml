info:
  name: 200LoginMustChangePassword
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/login"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: "{\r

      \    \"email\": \"andre.sousa@example.pt\",\r

      \    \"password\": \"password\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
