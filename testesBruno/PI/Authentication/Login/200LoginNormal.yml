info:
  name: 200LoginNormal
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/login"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: "{\r

      \    \"email\": \"miguel.ferreira@example.pt\",\r

      \    \"password\": \"password\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
