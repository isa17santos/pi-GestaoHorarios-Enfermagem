info:
  name: LoginNurse
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/login"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
          "email": "beatriz.sousa@example.pt",
          "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
