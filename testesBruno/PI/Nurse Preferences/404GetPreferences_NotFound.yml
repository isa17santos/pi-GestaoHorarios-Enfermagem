info:
  name: 404GetPreferences_NotFound
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/users/999999/preferences"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
