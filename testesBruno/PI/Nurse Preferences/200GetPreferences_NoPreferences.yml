info:
  name: 200GetPreferences_NoPreferences
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/users/2/preferences"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
