info:
  name: GetPreferences_Unauthenticated
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/users/2/preferences"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
