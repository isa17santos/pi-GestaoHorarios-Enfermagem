info:
  name: GetPreferences_ValidRequest
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/users/4/preferences"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
