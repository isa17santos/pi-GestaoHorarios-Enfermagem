info:
  name: GetPreferences_NoPreferences
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/users/2/preferences"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
