info:
  name: GetPreferences_NotFound
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/users/9999/preferences"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
