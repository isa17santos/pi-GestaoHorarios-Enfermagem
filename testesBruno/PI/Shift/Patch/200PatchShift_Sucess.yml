info:
  name: 200PatchShift_Sucess
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{base_url}}/shifts/1400"
  body:
    type: json
    data: |-
      {
          "schedule_id": 4,
          "shift_type_id": 1,
          "shift_date": "2026-08-04",
          "user_ids": [1, 2]
      }
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
