info:
  name: GetOtherUserShifts_ValidRequest
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/shifts?user_id=3&future=true"
  params:
    - name: user_id
      value: "3"
      type: query
    - name: future
      value: "true"
      type: query
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
