info:
  name: GetOtherUserShifts_ValidRequest
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/shifts?user_id=2&future=true"
  params:
    - name: user_id
      value: "2"
      type: query
    - name: future
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
