info:
  name: 200GetUserShifts_ValidRequest
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/shifts?mine=true&future=true"
  params:
    - name: mine
      value: "true"
      type: query
    - name: future
      value: "true"
      type: query
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
