info:
  name: 200DeleteShift
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/shifts/5?nurse_id=7"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: nurse_id
      value: "7"
      type: query
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
