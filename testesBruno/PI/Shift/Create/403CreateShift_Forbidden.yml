info:
  name: 403CreateShift_Forbidden
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/shifts"
  body:
    type: json
    data: |-
      {
          "schedule_id": 1,
          "shift_type_id": 1,
          "shift_date": "2026-04-07",
          "user_ids": [4, 5]
      }
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
