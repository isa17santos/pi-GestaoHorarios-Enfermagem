info:
  name: 200DashboardStatsHeadNurse
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/dashboard"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
