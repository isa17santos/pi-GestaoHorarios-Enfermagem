info:
  name: 403Statistics_Forbidden
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/statistics"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
