info:
  name: 401GetCertainShiftType
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/shift-types/1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
