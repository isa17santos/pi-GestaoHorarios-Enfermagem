info:
  name: 200DeleteShiftType_Success
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{base_url}}/shift-types/1"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
