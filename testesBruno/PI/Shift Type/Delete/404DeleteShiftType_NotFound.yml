info:
  name: 404DeleteShiftType_NotFound
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}/shift-types/9999999"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
