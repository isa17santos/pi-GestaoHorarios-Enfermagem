info:
  name: DeleteShiftType_Forbidden
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/shift-types/2"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
