info:
  name: DeleteShiftType_NotFound
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}/shift-types/9999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
