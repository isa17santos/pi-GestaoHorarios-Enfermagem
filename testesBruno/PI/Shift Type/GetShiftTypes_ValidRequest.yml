info:
  name: GetShiftTypes_ValidRequest
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/shift-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
