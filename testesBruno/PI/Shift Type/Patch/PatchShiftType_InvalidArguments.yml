info:
  name: PatchShiftType_InvalidArguments
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/shift-types/1"
  body:
    type: json
    data: "{\r\n\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
