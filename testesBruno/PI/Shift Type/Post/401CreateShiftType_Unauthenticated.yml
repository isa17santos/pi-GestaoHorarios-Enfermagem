info:
  name: 401CreateShiftType_Unauthenticated
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/shift-types"
  body:
    type: text
    data: "{\r\n\r\n}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
