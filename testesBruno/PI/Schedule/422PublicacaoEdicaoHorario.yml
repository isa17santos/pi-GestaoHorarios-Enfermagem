info:
  name: 422PublicacaoEdicaoHorario
  type: http
  seq: 11

http:
  method: POST
  url: "{{base_url}}/schedules/3/publish-edit"
  auth:
    type: bearer
    token: "{{token_head_nurse}}}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
