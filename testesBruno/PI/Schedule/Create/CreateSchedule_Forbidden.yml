info:
  name: CreateSchedule_Forbidden
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/schedules"
  body:
    type: json
    data: "{\r

      \  \"start_date\": \"2026-05-01\",\r

      \  \"end_date\": \"2026-05-31\"\r

      }"
  auth:
    type: bearer
    token: "{{api_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
