info:
  name: 422CreateSchedule_DuplicateMonth
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/schedules"
  body:
    type: json
    data: |-
      {
        "start_date": "2026-05-01",
        "end_date": "2026-05-31"
      }
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
