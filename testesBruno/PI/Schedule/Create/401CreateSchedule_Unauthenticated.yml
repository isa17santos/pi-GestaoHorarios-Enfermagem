info:
  name: 401CreateSchedule_Unauthenticated
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/schedules"
  body:
    type: json
    data: "{}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
