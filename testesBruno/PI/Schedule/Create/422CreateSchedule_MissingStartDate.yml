info:
  name: 422CreateSchedule_MissingStartDate
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/schedules"
  body:
    type: json
    data: "{\r

      \  \"end_date\": \"2026-05-31\"\r

      }"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
