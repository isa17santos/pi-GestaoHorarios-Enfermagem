info:
  name: PublishSchedule_InsufficientNurses
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/schedules/1/publish"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
