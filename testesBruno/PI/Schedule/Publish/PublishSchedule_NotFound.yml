info:
  name: PublishSchedule_NotFound
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/schedules/9999/publish"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
