info:
  name: PublishSchedule_AlreadyPublished
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{base_url}}/schedules/1/publish"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
