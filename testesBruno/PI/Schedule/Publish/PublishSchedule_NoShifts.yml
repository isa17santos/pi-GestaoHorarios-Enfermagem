info:
  name: PublishSchedule_NoShifts
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{base_url}}/schedules/5/publish"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
