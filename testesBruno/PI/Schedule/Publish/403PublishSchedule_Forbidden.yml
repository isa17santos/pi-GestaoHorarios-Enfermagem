info:
  name: 403PublishSchedule_Forbidden
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{base_url}}/schedules/1/publish"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
