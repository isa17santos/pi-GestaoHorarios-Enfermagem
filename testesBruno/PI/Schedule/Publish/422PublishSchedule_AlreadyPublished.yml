info:
  name: 422PublishSchedule_AlreadyPublished
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{base_url}}/schedules/1/publish"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
