info:
  name: 401PublishSchedule_Unauthenticated
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/schedules/1/publish"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
