info:
  name: 404PublishSchedule_NotFound
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{base_url}}/schedules/9999/publish"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
