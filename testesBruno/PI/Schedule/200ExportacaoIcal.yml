info:
  name: 200ExportacaoIcal
  type: http
  seq: 18

http:
  method: GET
  url: "{{base_url}}/schedules/ical"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
