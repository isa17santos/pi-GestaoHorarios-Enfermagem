info:
  name: 404PublicacaoEdicaoHorario
  type: http
  seq: 13

http:
  method: POST
  url: "{{base_url}}/schedules/30/publish-edit"
  auth:
    type: bearer
    token: "{{token_head_nurse}}}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
