info:
  name: 200VisualizarHorarioGlobal
  type: http
  seq: 14

http:
  method: GET
  url: "{{base_url}}/schedules/weekly?date=2026-07-18&view=global"
  params:
    - name: date
      value: 2026-07-18
      type: query
    - name: view
      value: global
      type: query
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
