info:
  name: 403IniciarEdicaoHorario
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/schedules/2/edit"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
