info:
  name: 403ExportacaoIcal
  type: http
  seq: 17

http:
  method: GET
  url: "{{base_url}}/schedules/ical"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
