info:
  name: 200VisualizarHorarioPessoal
  type: http
  seq: 14

http:
  method: GET
  url: "{{base_url}}/schedules/weekly"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
