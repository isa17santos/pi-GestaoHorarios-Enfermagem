info:
  name: DeleteSchedule_NotFound
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}/schedules/9999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
