info:
  name: DeleteSchedule_Success
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{base_url}}/schedules/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
