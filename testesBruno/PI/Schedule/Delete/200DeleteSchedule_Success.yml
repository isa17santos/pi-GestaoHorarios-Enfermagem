info:
  name: 200DeleteSchedule_Success
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{base_url}}/schedules/4"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
