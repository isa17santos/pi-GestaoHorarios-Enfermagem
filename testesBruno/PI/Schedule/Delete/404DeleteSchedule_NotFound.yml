info:
  name: 404DeleteSchedule_NotFound
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}/schedules/9999"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
