info:
  name: GetSchedules_Unauthenticated
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/schedules"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
