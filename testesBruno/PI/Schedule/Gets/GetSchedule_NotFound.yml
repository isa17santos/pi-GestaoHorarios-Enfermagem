info:
  name: GetSchedule_NotFound
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/schedules/9999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
