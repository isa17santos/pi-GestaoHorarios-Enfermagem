info:
  name: 200GetSchedules_Nurse
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/schedules"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
