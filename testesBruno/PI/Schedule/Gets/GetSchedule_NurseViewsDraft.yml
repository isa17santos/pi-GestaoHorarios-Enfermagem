info:
  name: GetSchedule_NurseViewsDraft
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/schedules/1"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
