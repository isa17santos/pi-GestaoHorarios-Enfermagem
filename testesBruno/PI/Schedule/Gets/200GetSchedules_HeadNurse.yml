info:
  name: 200GetSchedules_HeadNurse
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/schedules"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
