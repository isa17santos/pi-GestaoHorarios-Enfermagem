info:
  name: GetSchedule_ValidRequest
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/schedules/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
