info:
  name: GetSchedules_HeadNurse
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/schedules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
