info:
  name: 200GetSchedule_ValidRequest
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/schedules/1"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
