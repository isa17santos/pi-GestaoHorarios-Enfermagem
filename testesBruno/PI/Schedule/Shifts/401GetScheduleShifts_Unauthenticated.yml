info:
  name: 401GetScheduleShifts_Unauthenticated
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/schedules/1/shifts"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
