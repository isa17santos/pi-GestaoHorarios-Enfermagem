info:
  name: GetScheduleShifts_NotFound
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/schedules/9999/shifts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
