info:
  name: 404GetScheduleShifts_NotFound
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/schedules/9999/shifts"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
