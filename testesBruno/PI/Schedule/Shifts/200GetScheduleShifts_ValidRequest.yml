info:
  name: 200GetScheduleShifts_ValidRequest
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/schedules/1/shifts"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
