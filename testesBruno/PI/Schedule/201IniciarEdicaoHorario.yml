info:
  name: 201IniciarEdicaoHorario
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/schedules/2/edit"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
