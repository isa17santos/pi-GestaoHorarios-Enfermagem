info:
  name: 401ListNotifications
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/notifications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
