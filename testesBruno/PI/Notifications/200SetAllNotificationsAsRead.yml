info:
  name: 200SetAllNotificationsAsRead
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/notifications/read-all"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
