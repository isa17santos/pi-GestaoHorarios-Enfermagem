info:
  name: 200UnreadCount
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/notifications/unread-count"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
