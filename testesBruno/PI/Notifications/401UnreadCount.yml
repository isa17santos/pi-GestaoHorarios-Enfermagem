info:
  name: 401UnreadCount
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/notifications/unread-count"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
