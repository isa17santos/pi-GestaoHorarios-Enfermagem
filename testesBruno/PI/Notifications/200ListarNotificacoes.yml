info:
  name: 200ListarNotificacoes
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/notifications"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
