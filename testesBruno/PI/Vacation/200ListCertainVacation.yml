info:
  name: 200ListCertainVacation
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/vacations/1"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
