info:
  name: 200ListAllVacations
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/vacations"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
