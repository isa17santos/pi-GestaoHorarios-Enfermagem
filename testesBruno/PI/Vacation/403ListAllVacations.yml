info:
  name: 403ListAllVacations
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/vacations"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
