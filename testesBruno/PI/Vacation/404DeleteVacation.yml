info:
  name: 404DeleteVacation
  type: http
  seq: 17

http:
  method: DELETE
  url: "{{base_url}}/vacations/200000"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
