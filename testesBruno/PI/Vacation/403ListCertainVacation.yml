info:
  name: 403ListCertainVacation
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/vacations/1"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
