info:
  name: 404ListCertainVacation
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/vacations/100000000000000000"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
