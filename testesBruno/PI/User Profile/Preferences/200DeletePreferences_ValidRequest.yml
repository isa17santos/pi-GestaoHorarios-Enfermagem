info:
  name: 200DeletePreferences_ValidRequest
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/profile/preferences/9"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
