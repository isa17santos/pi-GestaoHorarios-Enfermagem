info:
  name: DeletePreference_NotFound
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}/profile/preferences/999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
