info:
  name: DeletePreferences_Forbidden
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{base_url}}/profile/preferences/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
