info:
  name: 401GetProfilePreferences_Unauthenticated
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/profile/preferences"
  headers:
    - name: Accept
      value: application/json
  body:
    type: text
    data: |-
      {
          "email": "beatriz.sousa@example.pt"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
