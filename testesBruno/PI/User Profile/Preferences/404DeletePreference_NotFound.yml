info:
  name: 404DeletePreference_NotFound
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}/profile/preferences/999999"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
