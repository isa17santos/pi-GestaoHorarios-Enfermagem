info:
  name: DeletePreferences_ValidRequest
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/profile/preferences/7"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
