info:
  name: 200GetProfilePreferences_ValidRequest
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/profile/preferences"
  headers:
    - name: Accept
      value: application/json
  body:
    type: text
    data: |-
      {
          "email": "beatriz.sousa@example.pt"
      }
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
