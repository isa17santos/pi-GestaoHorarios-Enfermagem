info:
  name: GetProfilePreferences_ValidRequest
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/profile/preferences"
  headers:
    - name: Accept
      value: application/json
  body:
    type: text
    data: "{\r

      \    \"email\": \"ana.antunes@example.pt\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
