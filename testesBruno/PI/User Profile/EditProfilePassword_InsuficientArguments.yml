info:
  name: EditProfilePassword_InsuficientArguments
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/profile"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: "{\r

      \    \"password\": \"Password@123\"\r

      \    \r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
