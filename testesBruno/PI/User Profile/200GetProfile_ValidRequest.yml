info:
  name: 200GetProfile_ValidRequest
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/profile"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
