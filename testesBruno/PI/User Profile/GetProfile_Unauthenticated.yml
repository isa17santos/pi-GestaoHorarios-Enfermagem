info:
  name: GetProfile_Unauthenticated
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/profile"
  headers:
    - name: Accept
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
