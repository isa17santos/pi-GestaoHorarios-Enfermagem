info:
  name: 422ChangePassword_InsuficientArguments
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/profile/change-password"
  body:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
