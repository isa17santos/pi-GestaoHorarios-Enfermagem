info:
  name: ChangePassword_Unconfirmed
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/profile/change-password"
  body:
    type: json
    data: |-
      {
          "current_password": "NovaPassword@456",
          "password": "password",
          "password_confirmation": "NovaPassword@999"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
