info:
  name: ChangePassword_WrongPassword
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/profile/change-password"
  body:
    type: json
    data: |-
      {
          "current_password": "passwordErrada",
          "password": "NovaPassword@456",
          "password_confirmation": "NovaPassword@456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
