info:
  name: 401ChangePassword_Unauthenticated
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/profile/change-password"
  body:
    type: json
    data: |-
      {
          "current_password": "NovaPassword@456",
          "password": "password",
          "password_confirmation": "password"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
