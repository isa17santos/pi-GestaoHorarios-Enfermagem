info:
  name: ChangePassword_SimplePassword
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/profile/change-password"
  body:
    type: json
    data: |-
      {
          "current_password": "NovaPassword@456",
          "password": "password",
          "password_confirmation": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
