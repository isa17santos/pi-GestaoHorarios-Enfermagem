info:
  name: ChangePassword_InsuficientArguments
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/profile/change-password"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
