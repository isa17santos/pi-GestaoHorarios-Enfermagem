info:
  name: 200ChangePassword_ValidRequest
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/profile/change-password"
  body:
    type: json
    data: |-
      {
          "current_password": "password",
          "password": "NovaPassword@456",
          "password_confirmation": "NovaPassword@456"
      }
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
