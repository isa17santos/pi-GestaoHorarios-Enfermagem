info:
  name: ChangePassword_SamePassword
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/profile/change-password"
  body:
    type: json
    data: |-
      {
          "current_password": "NovaPassword@456",
          "password": "NovaPassword@456",
          "password_confirmation": "NovaPassword@456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
