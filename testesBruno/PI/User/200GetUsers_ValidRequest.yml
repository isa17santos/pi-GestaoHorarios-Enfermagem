info:
  name: 200GetUsers_ValidRequest
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/users"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
