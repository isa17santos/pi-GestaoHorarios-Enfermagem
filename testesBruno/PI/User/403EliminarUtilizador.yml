info:
  name: 403EliminarUtilizador
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{base_url}}/users/3"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
