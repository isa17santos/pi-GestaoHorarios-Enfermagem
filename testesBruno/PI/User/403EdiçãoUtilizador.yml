info:
  name: 403EdiçãoUtilizador
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{base_url}}/users/13"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: "{\r

      \  \"name\": \"Mario Varela\",\r

      \  \"email\": \"mario.varela@example.pt\"\r

      }"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
