info:
  name: 200EliminarUtilizador
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{base_url}}/users/13"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
