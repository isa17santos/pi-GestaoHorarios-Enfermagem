info:
  name: 404DadosDeUmUser
  type: http
  seq: 14

http:
  method: GET
  url: "{{base_url}}/users/23333"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
