info:
  name: 422EdiçãoUtilizador
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{base_url}}/users/13"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: "{\r

      \  \"name\": \"Mario Varela\",\r

      \  \"email\": \"mario.varela\"\r

      }"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
