info:
  name: 200DadosDeUmUser
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/users/2"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
