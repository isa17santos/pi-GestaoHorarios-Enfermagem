info:
  name: 200SwapShift_CancelRequest
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/swaps/44/cancel"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
