info:
  name: SwapShift_ValidRequest
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/swaps"
  body:
    type: json
    data: |-
      {
          "offered_shift_ids": [410],
          "requested_shift_ids": [411],
          "target_user_id": 4,
          "notes": "Precisava de trocar este turno por motivos pessoais."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
