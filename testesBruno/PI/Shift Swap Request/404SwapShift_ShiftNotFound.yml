info:
  name: 404SwapShift_ShiftNotFound
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/swaps/999999/accept"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
