info:
  name: 200GetSwaps_ValidRequest
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/swaps"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
