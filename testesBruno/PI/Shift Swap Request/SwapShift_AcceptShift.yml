info:
  name: SwapShift_AcceptShift
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/swaps/4/accept"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
