info:
  name: 200GetSwapsValidate_ValidRequest
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/swaps/validate?offered_shift_id=1107&requested_shift_id=1104"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
