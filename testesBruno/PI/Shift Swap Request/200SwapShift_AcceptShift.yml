info:
  name: 200SwapShift_AcceptShift
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/swaps/71/accept"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
