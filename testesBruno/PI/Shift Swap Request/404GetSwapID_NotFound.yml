info:
  name: 404GetSwapID_NotFound
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/swaps/999999"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
