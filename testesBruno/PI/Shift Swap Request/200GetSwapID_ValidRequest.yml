info:
  name: 200GetSwapID_ValidRequest
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/swaps/44"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
