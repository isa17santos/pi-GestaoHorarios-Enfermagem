info:
  name: 403GetSwapID_Forbidden
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/swaps/44"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
