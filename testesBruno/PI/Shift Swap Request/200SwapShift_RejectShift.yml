info:
  name: 200SwapShift_RejectShift
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/swaps/72/reject"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
