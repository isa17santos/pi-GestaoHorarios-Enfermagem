info:
  name: 403SwapShift_Forbidden
  type: http
  seq: 16

http:
  method: POST
  url: "{{base_url}}/swaps/71/accept"
  auth:
    type: bearer
    token: "{{token_head_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
