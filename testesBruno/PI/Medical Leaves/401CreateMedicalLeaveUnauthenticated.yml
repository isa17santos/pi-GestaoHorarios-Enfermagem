info:
  name: 401CreateMedicalLeaveUnauthenticated
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/medical-leaves"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
