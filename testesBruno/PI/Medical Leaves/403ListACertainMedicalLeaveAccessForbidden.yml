info:
  name: 403ListACertainMedicalLeaveAccessForbidden
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/medical-leaves/4"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
