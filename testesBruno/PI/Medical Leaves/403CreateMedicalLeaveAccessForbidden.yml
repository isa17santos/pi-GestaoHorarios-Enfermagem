info:
  name: 403CreateMedicalLeaveAccessForbidden
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/medical-leaves"
  auth:
    type: bearer
    token: "{{token_nurse}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
