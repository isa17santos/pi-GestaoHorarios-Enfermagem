info:
  name: 404UpdateMedicalLeaveNotFound
  type: http
  seq: 14

http:
  method: PATCH
  url: "{{base_url}}/medical-leaves/10000"
  body:
    type: json
    data: |-
      {
        "user_id": 4,
        "start_date": "2026-06-26",
        "end_date": "2026-07-03",
        "reason": "Período prolongado de recuperação"
      }
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
