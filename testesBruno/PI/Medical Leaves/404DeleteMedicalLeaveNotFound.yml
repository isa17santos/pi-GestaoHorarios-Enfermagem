info:
  name: 404DeleteMedicalLeaveNotFound
  type: http
  seq: 17

http:
  method: DELETE
  url: "{{base_url}}/medical-leaves/400"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
