info:
  name: 401ListAllMedicalLeaves
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/medical-leaves"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
