info:
  name: 401UpdateMedicalLeaveUnauthenticated
  type: http
  seq: 16

http:
  method: PATCH
  url: "{{base_url}}/medical-leaves/1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
