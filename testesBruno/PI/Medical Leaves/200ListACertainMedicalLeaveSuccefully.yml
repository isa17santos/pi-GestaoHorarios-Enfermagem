info:
  name: 200ListACertainMedicalLeaveSuccefully
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}/medical-leaves/1"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
