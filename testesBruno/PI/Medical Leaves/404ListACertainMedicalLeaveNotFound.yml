info:
  name: 404ListACertainMedicalLeaveNotFound
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/medical-leaves/200"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
