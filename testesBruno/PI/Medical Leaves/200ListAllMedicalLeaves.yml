info:
  name: 200ListAllMedicalLeaves
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/medical-leaves"
  auth:
    type: bearer
    token: "{{token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
